[build]
  command = "export DEPLOY_TARGET=netlify && npm run build"
  publish = "out"

[build.environment]
  NODE_ENV = "production"
  DEPLOY_TARGET = "netlify"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
