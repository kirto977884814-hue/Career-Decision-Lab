[build]
  base = "career-decision-lab/"
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_ENV = "production"
  DEPLOY_TARGET = "netlify"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
